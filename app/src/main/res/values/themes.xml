<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SwiftCabAlpha" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_color</item> <!-- Ideally, define a distinct primary_container color -->
        <item name="colorOnPrimaryContainer">@color/black</item> <!-- Or a color that contrasts with primary_container -->

        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/secondary_color</item> <!-- Ideally, define a distinct secondary_container color -->
        <item name="colorOnSecondaryContainer">@color/black</item> <!-- Or a color that contrasts with secondary_container -->
    </style>

    <style name="Theme.SwiftCabAlpha" parent="Base.Theme.SwiftCabAlpha" />

    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar"> <!-- Changed parent here -->
        <item name="navigationViewStyle">@style/MyNavigationViewStyle</item>
    </style>

    <style name="MyNavigationViewStyle" parent="Widget.Material3.NavigationView">
        <item name="itemBackground">@drawable/nav_item_background</item>
        <item name="itemTextColor">@color/nav_item_text_color_selector</item>
        <item name="itemIconTint">@color/nav_item_icon_tint_selector</item>
        <item name="itemHorizontalPadding">32dp</item>
    </style>

    <style name="ShapeAppearance.App.SmallComponent" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>
</resources>
