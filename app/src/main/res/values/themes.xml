<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SwiftCabAlpha" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/primary_color</item> <!-- Ideally, define a distinct primary_container color -->
        <item name="colorOnPrimaryContainer">@color/black</item> <!-- Or a color that contrasts with primary_container -->

        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/secondary_color</item> <!-- Ideally, define a distinct secondary_container color -->
        <item name="colorOnSecondaryContainer">@color/black</item> <!-- Or a color that contrasts with secondary_container -->

        <!-- App-wide widget styles for consistent UI -->
        <item name="buttonStyle">@style/Widget.SwiftCab.Button</item>
        <item name="materialButtonStyle">@style/Widget.SwiftCab.MaterialButton</item>
        <item name="textInputStyle">@style/Widget.SwiftCab.TextInputLayout</item>
        <item name="toolbarStyle">@style/Widget.SwiftCab.Toolbar</item>
        <item name="navigationViewStyle">@style/MyNavigationViewStyle</item>
        <item name="ratingBarStyle">@style/Widget.SwiftCab.RatingBar</item>
        <!-- Removed cardViewStyle to avoid invalid parent reference; CardView widgets will use default style -->
    </style>

    <style name="Theme.SwiftCabAlpha" parent="Base.Theme.SwiftCabAlpha" />

    <!-- Legacy AppTheme kept for compatibility but not used by manifest -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="navigationViewStyle">@style/MyNavigationViewStyle</item>
    </style>

    <style name="MyNavigationViewStyle" parent="Widget.Material3.NavigationView">
        <item name="itemBackground">?attr/selectableItemBackground</item>
        <item name="itemTextColor">@color/nav_item_text_color_selector</item>
        <item name="itemIconTint">@color/nav_item_icon_tint_selector</item>
        <item name="itemHorizontalPadding">16dp</item>
    </style>

    <!-- Toolbar Style -->
    <style name="Widget.SwiftCab.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="titleTextColor">?attr/colorOnPrimary</item>
        <item name="subtitleTextColor">?attr/colorOnPrimary</item>
    </style>

    <!-- Standard Button Style -->
    <style name="Widget.SwiftCab.Button" parent="Widget.AppCompat.Button">
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">?attr/colorPrimary</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <!-- MaterialButton Style -->
    <style name="Widget.SwiftCab.MaterialButton" parent="Widget.Material3.Button">
        <item name="android:minHeight">48dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">8dp</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="rippleColor">?attr/colorPrimaryContainer</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- TextInputLayout Style -->
    <style name="Widget.SwiftCab.TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">?attr/colorPrimary</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="hintTextColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <!-- Global RatingBar Style -->
    <style name="Widget.SwiftCab.RatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressTint">#FFC107</item>
        <item name="android:secondaryProgressTint">#FFD54F</item>
        <item name="android:progressBackgroundTint">#BDBDBD</item>
    </style>

    <style name="ShapeAppearance.App.SmallComponent" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>
</resources>